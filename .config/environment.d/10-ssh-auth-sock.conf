# Bitwarden desktop's SSH agent, so GUI apps and systemd user services see the
# same agent the shell does. Native (rpm) install path; Flatpak/snap differ.
SSH_AUTH_SOCK=${HOME}/.bitwarden-ssh-agent.sock
